apiVersion: 1

datasources:

  - name: influxdb
    type: influxdb
    access: proxy
    database: $INFLUXDB_DB
    url: $INFLUXDB_URL
    user: $INFLUXDB_USERNAME
    secureJsonData:
      password: $INFLUXDB_PASSWORD
